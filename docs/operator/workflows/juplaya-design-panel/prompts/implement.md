# Implement — execute the accepted design

Apply the accepted synthesis to the repo (write_scope covers `docs/` and your run dir):
1. **Doc divorce:** create the per-domain docs per the accepted split map; move content faithfully (no loss, no paraphrase-drift of settled facts); reduce `docs/juplaya-trailer-context.md` to the slim index (settled trailer identity, links to domain docs + specs + mission, weight note, standing preferences).
2. **Decisions:** append the bus, fridge, and awning decisions to `docs/DECISION_LOG.md` (D006+, status `proposed`, owner ratifies) and reflect them in the relevant domain docs.
3. Update cross-references (mission doc pointer, spec-sheet links) so nothing dangles.
4. Do NOT git-commit; the operator commits. Write an implementation summary to your declared artifact. Stay live for the build-review interrogation.
