<!-- Generated by `striatum init --with-ddd-layout` (RFC 0021).
     Edit freely; this file is not regenerated. -->

# SPEC

## Why this file exists

`SPEC.md` is the implementation contract for this project. It
records what the project does, what state it owns, and what it
deliberately does not model. Together with `PRD.md`, it forms
the boundary contract that domain decisions cite.

## What the project does

TODO — describe the project's behavior at a level a contributor
can reproduce. Keep it concrete: which inputs, which outputs,
which invariants the code enforces.

## State the project owns

TODO — list the state stores (databases, files, queues) the
project considers durable. Cite the SQLite tables, the on-disk
artifacts, the message buses. Anything not listed here is not
part of the project's bounded context.

## What the project does NOT model

TODO — list the things the project deliberately leaves to other
domains. Examples: external service health, downstream
deployment, the user's intent, the agent's reasoning. The
boundary is what makes the model coherent — be explicit.

## Invariants

TODO — list the invariants the codebase enforces. Each invariant
should be testable. Examples: "every state transition appends an
event," "no foreign mutation of `<table>`," "configuration is
read once at startup."

## Schemas

TODO — link to the canonical schemas (workflow, message, artifact
front matter) the project relies on.

## Initialization

TODO — describe the on-disk layout `<project> init` produces and
what each file/directory is for.
